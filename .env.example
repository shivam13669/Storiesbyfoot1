# Supabase Configuration
# Get these values from your Supabase project dashboard
# Settings → API → Project URL and Anon Key

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env.local to Git - it contains secrets!
# 2. .env.local is already in .gitignore
# 3. Only .env.example should be committed
# 4. Copy .env.example to .env.local and fill in your actual values
# 5. Each developer/environment should have their own .env.local
